import express from 'express';

const router = express.Router();

// Placeholder routes - will be implemented with notification functionality
router.get('/', (req, res) => {
  res.json([]);
});

export default router;